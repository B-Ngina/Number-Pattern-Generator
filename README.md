# Number-Pattern-Generator
A logic-based function that generates a space-separated sequence of numbers. 
**Input Validation**: Ensures the argument is a positive integer. 
**Usage**: `number_pattern(5)` returns `"1 2 3 4 5"`.
